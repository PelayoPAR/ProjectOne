class Background {
  constructor() {
    this.left = 0;
  }

  preload() {
    // this.img = loadImage("")
  }

  drawBackground() {
    background("blue");
    // createCanvas(500, 750);
    // image(this.img, this.left, 0, CANVAS_WIDTH, CANVAS_HEIGHT)
  }
}
